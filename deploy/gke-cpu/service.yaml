apiVersion: v1
kind: Service
metadata:
  name: inference-engine
spec:
  type: LoadBalancer        # external IP to hit the server from anywhere
  selector:
    app: inference-engine
  ports:
    - port: 80              # external port
      targetPort: 8000      # container (uvicorn) port
      protocol: TCP
